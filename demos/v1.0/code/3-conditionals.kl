class program {
    program() {
        if (facingEast && frontIsClear) {
            move()
        } else {
            turnleft()
        }
        
        if (!frontIsBlocked) {
            move()
            turnleft()
            move()
        }
        
        turnoff()
    }
}